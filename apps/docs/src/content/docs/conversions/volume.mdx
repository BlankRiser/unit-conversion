---
title: "Volume"
template: doc
---
The Volume category converts between metric liters/milliliters and Imperial/US gallons, quarts, pints, cups, and fluid ounces.

## Supported Units

- Metric: milliliter `ml`, liter `l`
- Imperial / US: fluid ounce `fl oz`, cup, pint `pt`, quart `qt`, gallon `gal`

## Quick Example
```ts
import { Conversion } from "@devhaven/unit-conversion";

const convert = new Conversion();

convert(1000).from("milliliter").to("liter"); 
convert(1).from("gallon").to("liter"); 

```

## Common Conversions

### Liter ↔ Milliliter
```ts
convert(1).from("liter").to("milliliter");      
convert(1).from("milliliter").to("liter");      
```

### Liter ↔ Cubic Meter
```ts
convert(1).from("liter").to("cubic-meter");      
convert(1).from("cubic-meter").to("liter");      
```

### Cup (US) ↔ Milliliter
```ts
convert(1).from("us-legal-cup").to("milliliter");      
convert(1).from("milliliter").to("us-legal-cup");      
```